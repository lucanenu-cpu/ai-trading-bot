[phases.setup]
aptPkgs = ["libgomp1"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 180 server:app"
